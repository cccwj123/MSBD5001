Language used: python / jupyter
required packages: pandas, seaborn, numpy, sklearn, matplotlib

In order to run the program, please start jupyter notebook after cloning out the repository, and run the whole notebook, to generate the test.csv

